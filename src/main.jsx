import { useEffect, useMemo, useRef, useState } from 'react'
import { createRoot } from 'react-dom/client'
import html2canvas from 'html2canvas'
import { jsPDF } from 'jspdf'
import './styles.css'

const services = ['Aéreos Buenos Aires - Punta Cana con carry on', '7 noches de alojamiento con desayuno en Coconut inn', 'Traslados aeropuerto - hotel - aeropuerto', 'Auto toda la estadía para recorrer la isla', 'Ed Card']
const logo = './assets/reference-image.png'

function WhatsappIcon() { return <svg className="brand-icon" viewBox="0 0 448 512" aria-label="WhatsApp"><path fill="currentColor" d="M380.9 97.1C339 55.1 283.2 32 223.9 32c-122.4 0-222 99.6-222 222 0 39.1 10.2 77.3 29.6 111L0 480l117.7-30.9c32.4 17.7 68.9 27 106.1 27h.1c122.3 0 224.1-99.6 224.1-222 0-59.3-25.2-115-67.1-157zm-157 341.6c-33.2 0-65.7-8.9-94-25.7l-6.7-4-69.8 18.3L72 359.2l-4.4-7c-18.5-29.4-28.2-63.3-28.2-98.2 0-101.7 82.8-184.5 184.6-184.5 49.3 0 95.6 19.2 130.4 54.1 34.8 34.9 56.2 81.2 56.1 130.5 0 101.8-84.9 184.6-186.6 184.6zm101.9-138c-5.6-2.8-33.1-16.3-38.2-18.2-5.1-1.9-8.8-2.8-12.5 2.8s-14.4 18.2-17.7 21.9c-3.2 3.7-6.5 4.2-12.1 1.4-33.2-16.6-55-29.7-76.9-67.4-5.8-10 5.8-9.3 16.6-31 1.9-3.7.9-6.9-.5-9.7-1.4-2.8-12.5-30.1-17.1-41.2-4.5-10.8-9.1-9.3-12.5-9.5-3.2-.2-6.9-.2-10.6-.2-3.7 0-9.7 1.4-14.8 6.9-5.1 5.6-19.4 19-19.4 46.3s19.9 53.7 22.6 57.4c2.8 3.7 39.1 59.7 94.8 83.8 13.2 5.7 23.5 9.1 31.5 11.6 13.2 4.2 25.2 3.6 34.7 2.2 10.6-1.6 33.1-13.5 37.7-26.6 4.6-13.1 4.6-24.3 3.2-26.6-1.3-2.5-5-3.9-10.6-6.6z"/></svg> }
function InstagramIcon() { return <svg className="brand-icon" viewBox="0 0 448 512" aria-label="Instagram"><path fill="currentColor" d="M224.1 141c-63.6 0-114.9 51.3-114.9 114.9s51.3 114.9 114.9 114.9S339 319.5 339 255.9 287.7 141 224.1 141zm0 189.6c-41.1 0-74.7-33.5-74.7-74.7s33.5-74.7 74.7-74.7 74.7 33.5 74.7 74.7-33.6 74.7-74.7 74.7zm146.4-194.3c0 14.9-12 26.8-26.8 26.8-14.9 0-26.8-12-26.8-26.8s12-26.8 26.8-26.8 26.8 12 26.8 26.8zm76.1 27.2c-1.7-35.9-9.9-67.7-36.2-93.9-26.2-26.2-58-34.4-93.9-36.2-37-2.1-147.9-2.1-184.9 0-35.8 1.7-67.6 9.9-93.9 36.1s-34.4 58-36.2 93.9c-2.1 37-2.1 147.9 0 184.9 1.7 35.9 9.9 67.7 36.2 93.9s58 34.4 93.9 36.2c37 2.1 147.9 2.1 184.9 0 35.9-1.7 67.7-9.9 93.9-36.2 26.2-26.2 34.4-58 36.2-93.9 2.1-37 2.1-147.8 0-184.8zM398.8 388c-7.8 19.6-22.9 34.7-42.6 42.6-29.5 11.7-99.5 9-132.1 9s-102.7 2.6-132.1-9c-19.6-7.8-34.7-22.9-42.6-42.6-11.7-29.5-9-99.5-9-132.1s-2.6-102.7 9-132.1c7.8-19.6 22.9-34.7 42.6-42.6 29.5-11.7 99.5-9 132.1-9s102.7-2.6 132.1 9c19.6 7.8 34.7 22.9 42.6 42.6 11.7 29.5 9 99.5 9 132.1s2.7 102.7-9 132.1z"/></svg> }
function FlightBanner() { return <div className="quote-banner"><svg viewBox="0 0 250 112" aria-hidden="true"><path d="M12 24C66 34 75 89 166 76" fill="none" stroke="white" strokeWidth="3" strokeDasharray="9 10" strokeLinecap="round"/><path d="M151 61c3 1 8 4 14 8l28-7c5-1 8 0 9 2s-1 5-5 7l-28 10-6 25c-1 5-3 8-5 8s-3-4-3-8l1-22-17-8-9 7-5-1 9-11-2-13 3-1 8 10 7-3z" fill="white"/></svg><strong>COTIZACIÓN</strong></div> }

function App() {
  const [quote, setQuote] = useState({ destination: 'ARUBA', nights: '7', dates: '21/11 al 28/11', price: '1390' })
  const [selected, setSelected] = useState(services)
  const [flightImages, setFlightImages] = useState([])
  const [exporting, setExporting] = useState(false)
  const [mobileView, setMobileView] = useState('form')
  const [scale, setScale] = useState(1)
  const sheetRef = useRef(null)
  const previewRef = useRef(null)
  const update = (key) => (event) => setQuote((current) => ({ ...current, [key]: event.target.value }))
  const output = useMemo(() => ({ destination: quote.destination.trim().toUpperCase() || 'DESTINO', nights: quote.nights || '0', dates: quote.dates.trim().toUpperCase() || 'FECHAS A DEFINIR', price: quote.price.trim().replace(/^USD\s*/i, '') || '0' }), [quote])

  useEffect(() => {
    const resize = () => { const box = previewRef.current; if (!box) return; setScale(Math.min(1, (box.clientWidth - 24) / 595, (box.clientHeight - 18) / 842)) }
    resize(); const observer = new ResizeObserver(resize); if (previewRef.current) observer.observe(previewRef.current); window.addEventListener('resize', resize); return () => { observer.disconnect(); window.removeEventListener('resize', resize) }
  }, [])
  const toggleService = (service) => setSelected((current) => current.includes(service) ? current.filter((item) => item !== service) : [...current, service])
  function chooseFlights(event) { const files = [...(event.target.files || [])]; if (!files.length) return; Promise.all(files.map((file) => new Promise((resolve) => { const reader = new FileReader(); reader.onload = () => resolve({ id: `${file.name}-${file.lastModified}-${Math.random()}`, name: file.name, src: reader.result }); reader.readAsDataURL(file) }))).then((items) => setFlightImages((current) => [...current, ...items])); event.target.value = '' }
  const removeFlight = (id) => setFlightImages((current) => current.filter((image) => image.id !== id))
  async function exportPdf() { if (!sheetRef.current) return; setExporting(true); try { const canvas = await html2canvas(sheetRef.current, { scale: 2, backgroundColor: '#fff', useCORS: true }); const pdf = new jsPDF({ orientation: 'portrait', unit: 'mm', format: 'a4' }); pdf.addImage(canvas.toDataURL('image/jpeg', .95), 'JPEG', 0, 0, 210, 297); pdf.save(`Cotizacion-${output.destination}.pdf`) } catch (error) { console.error(error); window.alert('No se pudo crear el PDF. Intentá nuevamente.') } finally { setExporting(false) } }
  return <><header className="topbar"><div className="brand"><img src={logo} alt="Turismo Explora"/><span>Cotizador</span></div><button onClick={exportPdf} className="btn-primary" disabled={exporting}>{exporting ? 'Generando PDF...' : 'Descargar PDF'}</button></header><nav className="mobile-tabs"><button className={mobileView === 'form' ? 'active' : ''} onClick={() => setMobileView('form')}>Cotizador</button><button className={mobileView === 'preview' ? 'active' : ''} onClick={() => setMobileView('preview')}>Vista previa</button></nav><main className={`workspace ${mobileView === 'preview' ? 'show-preview' : 'show-form'}`}><aside className="form-panel"><div className="form-heading"><p className="eyebrow">Nueva cotización</p><h1>Armá tu propuesta</h1><p>Completá los datos y descargá una cotización lista para enviar.</p></div><form onSubmit={(event) => event.preventDefault()}><label>Destino<input value={quote.destination} onChange={update('destination')}/></label><div className="fields-2"><label>Noches<input type="number" min="1" value={quote.nights} onChange={update('nights')}/></label><label>Fechas<input value={quote.dates} onChange={update('dates')}/></label></div><label className="upload-label">Imágenes con detalle de vuelos<span className="file-picker"><input type="file" accept="image/*" multiple onChange={chooseFlights}/>Agregar imágenes</span></label>{flightImages.length ? <div className="upload-list">{flightImages.map((image) => <span key={image.id}>{image.name}<button type="button" onClick={() => removeFlight(image.id)} aria-label={`Quitar ${image.name}`}>×</button></span>)}</div> : <p className="hint">Podés cargar ida, vuelta y cualquier detalle adicional.</p>}<fieldset><legend>Incluye</legend>{services.map((service) => <label className="check" key={service}><input type="checkbox" checked={selected.includes(service)} onChange={() => toggleService(service)}/><span>{service}</span></label>)}</fieldset><label>Precio total por persona (base doble)<span className="price-field"><span>USD</span><input value={quote.price} onChange={update('price')} inputMode="numeric"/></span></label></form></aside><section className="preview-area"><div className="preview-label"><span>Vista previa</span><small>Formato A4</small></div><div className="preview-stage" ref={previewRef}><article ref={sheetRef} className="pdf-sheet" style={{ '--sheet-scale': scale }}><div className="sheet-header"><FlightBanner/></div><div className="sheet-content"><div className="trip-title"><h2>{output.destination}</h2><span>{output.nights} NOCHES</span><i/><span>{output.dates}</span></div><section className="flight-section"><h3>Detalle vuelos</h3>{flightImages.length ? <div className="flight-gallery">{flightImages.map((image) => <img src={image.src} key={image.id} alt={image.name}/>)}</div> : <div className="flight-placeholder"><span>✈</span><strong>Detalle de vuelos</strong><small>Adjuntá una o más imágenes desde el formulario</small></div>}</section><section className="included-section"><h3>INCLUYE</h3><ul>{selected.length ? selected.map((service) => <li key={service}>{service}</li>) : <li>Consultá los servicios incluidos</li>}</ul></section><section className="price-section"><p>Total por persona en base doble: <strong>USD {output.price}</strong></p></section><section className="payment-section"><h3>FORMAS DE PAGO</h3><p>Anticipo USD 600 por persona para emitir aéreos y congelar el precio. Saldo en cuotas no necesariamente fijas, debe estar abonado el total 15 días antes del viaje.</p><p className="disclaimer">TARIFAS SUJETAS A DISPONIBILIDAD O MODIFICACIÓN AL MOMENTO DE LA RESERVA.</p></section></div><footer className="sheet-footer"><img src={logo} alt="Turismo Explora"/><div className="social-lines"><span><WhatsappIcon/>+54 9 261 777 0250</span><span><InstagramIcon/>@turismoexplora_</span></div></footer></article></div></section></main></>
}
createRoot(document.getElementById('root')).render(<App />)
